import * as RadixDialog from '@radix-ui/react-dialog'

const { Root, Trigger, Portal, Overlay, Content, Title, Description, Close } = RadixDialog

export { Close, Content, Description, Overlay, Portal, Root, Title, Trigger }
