import { ArgsTable, Title,  Controls, Meta, Story, Canvas } from '@storybook/addon-docs';
import { IconButton, Button } from '@custompackages/designsystem';
import TestIcon from './TestIcon.tsx'

<Meta title="Example/Button"
      component={Button}
      tags={['autodocs']} 
      argTypes={{
        
      }}
      args={{
        variant: 'primary',
        size : 'medium'
      }}
/>

<Title />

## 설치

프로젝트에 @custompackages/designsystem이 설치되어 있는지 확인하세요. 

다음과 같이 Button 컴포넌트를 임포트할 수 있습니다

```jsx
import { Button } from '@custompackages/designsystem';
```

## Basic Usage

<Canvas>
  <Story name="BasicButton" args={{ children: 'text' }}>
    {args => <Button {...args} />}
  </Story>
</Canvas>

## Props

<Controls />

## Button With Left Icon
 
<Canvas>
  <Story name="ButtonWithLeftIcon" args={{ children: 'text', LeftIcon: <TestIcon/> }}>
    {args => <Button {...args} />}
  </Story>
</Canvas>

버튼 텍스트의 왼쪽에 아이콘을 추가하는 방법


## Button With Right Icon
 
<Canvas>
  <Story name="ButtonWithRightIcon" args={{ children: 'text', RightIcon: <TestIcon/> }}>
    {args => <Button {...args} />}
  </Story>
</Canvas>

마찬가지로, 버튼 텍스트의 오른쪽에 아이콘을 추가하는 방법:




