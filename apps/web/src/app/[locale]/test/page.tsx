import { Counter } from '@custompackages/modules'

const Page = () => {
  return (
    <main className="flex flex-col min-h-screen p-6">
      <Counter />
    </main>
  )
}

export default Page
